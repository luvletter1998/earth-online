{
  "buildCommand": "cd client/tmp_client && npm install && npm run build",
  "outputDirectory": "client/tmp_client/dist",
  "installCommand": "cd client/tmp_client && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
